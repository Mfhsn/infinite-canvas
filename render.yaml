services:
  - type: web
    name: infinite-canvas
    runtime: docker
    plan: free
    autoDeployTrigger: off
    healthCheckPath: /api/storage/health
    envVars:
      - key: PORT
        value: 3000
