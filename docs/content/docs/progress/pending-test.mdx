---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- 首页创作启动台：替换原大标题与远程提示词展示，新增新建/继续画布、五类工作台入口、本地最近画布和条件式最近素材；需验证空数据/有数据、离线、浅色/深色、中英文和移动端布局。
- 多语言界面：已覆盖全部 Web 路由、Canvas 节点/工具栏/弹窗、本地与在线 Agent 可见日志、WebDAV 进度、生成记录和本地可预期错误；切换时不刷新，不改写用户内容或 AI system prompt/tool protocol。自动检查已通过，仍需在 390px/桌面宽度、深浅色下手工验证英文溢出及切换后的 Toast/弹窗。
- 多语言数据兼容：新生成记录仅保存 `pending | success | failed` 与时间戳，WebDAV/Agent/画布错误保存稳定 code；需用历史 `生成中/成功/失败` 记录和旧画布做一次真实浏览器迁移验收。
- 环境文件配置：新增统一 `VITE_*` 渠道/模型变量、`env.js` 运行时注入和 Docker 启动脚本，可通过 `.env` / `web/.env.local` 配置默认 API 渠道、模型与 Dream `platform_id`；需验证本地 Vite、Docker Compose 和静态部署覆盖行为。
- API接口示例调用格式：取消原 OpenAI 默认渠道，只内置“API接口示例”渠道；图片默认使用 `doubao-seedream-4.5`，视频默认使用 `doubao-seedance-1-5-pro-251215`，并接入 Dream 图片、局部重绘/扩图、Dream 视频和 TTS 端点；图片创建端点返回任务 ID 时会继续轮询状态和结果。实测当前部署要求 Bearer `access_token`，仍需用有效令牌验证真实生成结果和额度扣除。
- Dream 视频模型：`doubao-seedance-1-5-pro-251215` 收窄为首尾帧模式，`doubao-seedance-2-0-260128` 支持首尾帧和 `reference2video` 全能参考；提示词统一必填，全能参考支持 9 张图片、3 个视频、3 个音频及 `@图片1` / `@视频1` / `@音频1` 引用。图片和音频使用 `usage=mixcut` 直接上传，视频使用 5 MiB 分片上传并合并为素材 ID；已通过请求构造、分片上传、轮询和视觉回归检查，仍需用有效 Token 完成真实生成验收。
- 画布参考图上传：图片节点提示卡支持一次选择多张参考图，上传后自动创建图片节点并连入目标节点，卡片展示数量、缩略图和移除入口；上传接口按文档携带 `usage=mixcut`，真实浏览器重试已完成素材上传并生成图片。FastAPI 422 对象数组和旧画布对象型错误会统一转成文本，不再导致重试后黑屏；仍需用户环境复测现有失败节点。
- Codex App 插件文档：新增插件安装教程，区分 AI 自动安装、手动安装和本仓库开发调试安装；同步优化插件 README 与技能说明。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- WebDAV 同步：移除 Next.js 转发代理和连接方式选择，改为浏览器前端直接连接 WebDAV 服务。
- 提示词库：移除 Next.js route 抓取和服务端内存缓存，改为浏览器前端直连 GitHub 原始文件并缓存到 IndexedDB。
- 提示词库：移除已 404 的 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` 来源。
- 前端构建：移除 Next.js，改为 Vite + React Router，并将前端目录调整为 `pages` / `components` / `stores` / `lib` / `layouts` / `styles`，需验证首页、图片/视频工作台、素材、提示词库和画布路由跳转。
- 前端构建：修复 TypeScript 严格检查下的构建报错。
- Vercel 部署：新增 `web` 和 `docs` 两个子项目的 Vercel 配置，需分别验证静态前端和文档站部署。
- Docker 部署：运行镜像从 Node serve 改为 nginx 静态托管，需验证前端路由刷新回退。
- 环境变量：移除过期的后端 `.env.example` 示例文件。
